import { SinCaracterEspecial } from './validacion-caracteresespeciales.directive';

describe('ValidacionCaracteresespecialesDirective', () => {
  it('should create an instance', () => {
    const directive = new SinCaracterEspecial();
    expect(directive).toBeTruthy();
  });
});
