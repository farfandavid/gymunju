import { ValidacionPasswordDirective } from './validacion-password.directive';

describe('ValidacionPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidacionPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
